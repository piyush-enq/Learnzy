// import PropTypes from 'prop-types';
// // @mui
// import { Grid } from '@mui/material';

// import StudentAssignCard from './StudentAssignCard';
// // ----------------------------------------------------------------------

// StudentAssignCardList.propTypes = {
//   assignments: PropTypes.array.isRequired,
// };

// export default function StudentAssignCardList({ assignments}) {
//   return (
//     <Grid container spacing={3}>
//       {assignments.map((assignment) => (
//         <Grid key={assignment.id} item xs={12} sm={6} md={3}>
//           <StudentAssignCard assignment={assignment} />
//         </Grid>
//       ))}
//     </Grid>
//   );
// }
